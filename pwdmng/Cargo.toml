[package]
name = "pwdmng"
version = "0.1.0"
edition = "2021"

[dependencies]
tui = "0.19.0"          # TUI
crossterm = "0.25.0"    # terminal back-end
arboard = "3.2.0"       # copy text to clipboardcar